version: '3'
services:
  web:
    image: greg78/node:latest
    ports:
      - "3000:3000"
    volumes:
      - D:\www\local.dev\bank.io\API:/usr/src/app
    links:
      - db
    command: npm start
  db:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data: